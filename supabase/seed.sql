-- Seed data is applied after migrations during `supabase db reset`.
-- Since guestbook entries require auth.users rows (via the trigger),
-- real seed data should be created through the app or Supabase Studio.
-- This file is intentionally minimal.

-- You can test locally by signing up through the app, which will
-- auto-create a profile via the on_auth_user_created trigger.
